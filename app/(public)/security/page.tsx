import { buildMetadata } from "@/lib/seo";
import SecurityPage from ".";

export const metadata = buildMetadata({
  title: "Security",
  description:
    "Learn about ClientFlow's security practices. Understand how we protect your data, our commitment to a secure platform, and the measures we take to ensure user safety.",
  path: "/security",
});

export default SecurityPage;
